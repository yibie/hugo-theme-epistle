{
  "name": "epistle-guestbook-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "node --test test/*.test.mjs && npm run test --workspaces --if-present",
    "test:hugo": "hugo --source exampleSite --themesDir ../.. --destination /tmp/epistle-guestbook-build && grep -q 'class=\"correspondence\"' /tmp/epistle-guestbook-build/posts/about-that-afternoon/index.html && grep -q '这是一条来自 front matter 的留言' /tmp/epistle-guestbook-build/posts/about-that-afternoon/index.html && ! grep -q '这是一条来自 front matter 的留言' /tmp/epistle-guestbook-build/posts/spring-rain/index.html && grep -q 'class=\"page-guestbook\"' /tmp/epistle-guestbook-build/guestbook/index.html && grep -q 'name=\"publish_consent\" value=\"true\"' /tmp/epistle-guestbook-build/posts/spring-rain/index.html && ! grep -q 'id=\"guestbook-consent\"' /tmp/epistle-guestbook-build/posts/spring-rain/index.html && grep -q '来信有可能被摘录公开' /tmp/epistle-guestbook-build/posts/spring-rain/index.html"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  },
  "engines": {
    "node": ">=22"
  }
}
